# typos configuration file for spell checking
# https://github.com/crate-ci/typos

[files]
extend-exclude = [
    "target/",
    "Cargo.lock",
    ".git/",
    "*.svg",
    "*.png",
    "*.jpg",
    "index.html",
    "*.cast",
    "web/dist",
]

[default]
locale = "en"

[default.extend-words]
# Project-specific words that should not be reported as typos.
# Example: helo = "helo"
iif = "iif"
oif = "oif"
ratatui = "ratatui"
