# Codex Provider

This fixture declares registry metadata for the Codex CLI integration.

Capabilities are declarative metadata only. They do not grant directory access,
confirm the CLI is installed, or authorize execution.

This provider may send prompts and code context to OpenAI services when the CLI
is executed by a future runtime phase.
