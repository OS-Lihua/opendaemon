# Generic Test Provider

This fixture exists to keep registry validation and provider API tests stable.

Capabilities are declarative metadata only. They do not grant directory access,
confirm the executable is installed, or authorize execution.

The fixture does not declare remote execution and does not declare code upload
to a vendor service.
